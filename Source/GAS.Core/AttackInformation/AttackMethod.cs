namespace GAS.Core.AttackInformation {
    public enum AttackMethod {
        TCP,
        UDP,
        HTTP,
        ReCoil,
        SlowLOIC,
        RefRef,
        AhrDosme,
        Post,
        TMOF
    }
}
